// project import
import support from './support';
import users from './users';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [users, support]
};

export default menuItems;
