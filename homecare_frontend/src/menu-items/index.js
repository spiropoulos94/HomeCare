// project import
import support from './support';
import users from './users';
import reports from './reports';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [reports, users, support]
};

export default menuItems;
